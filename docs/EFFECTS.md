# 演出の棚卸し（黒猫飯店）

ゲーム内で「何かが起きた」と伝えるべき瞬間の全リスト。実装状況と、
**作るのか外部から持ってくるのか**の判断を書く。2026-07-28 時点。

---

## 結論：視覚エフェクトは全部「作る」、音は「もう持っている」

**外部アセットは使わない。** 理由は3つ。

1. **ピクセル密度とパレットの規律が壊れる。** このセッションで最も効いた規律が
   「1テクセルあたりの画面pxを混ぜない」「有彩色は識別色1つ＋状態色まで」だった。
   配布されているVFXパックは各々の密度とパレットを持っているので、混ぜた瞬間に
   HD-2Dは安く見える。ここは妥協点が無い。
2. **既存の演出が全て手続き描画。** `draw_colored_polygon` / `draw_circle` /
   `draw_texture_rect` で毎フレーム描いている。解像度に依存せず、パレットの色を
   そのまま使え、アセットの読み込みも要らない。スプライトシートを混ぜると
   この一貫性が切れる。
3. **そもそも取得できない。** この実行環境のネットワークポリシーは
   kenney.nl / opengameart.org / godotengine.org/asset-library をいずれも
   403 で拒否する（`$HTTPS_PROXY/__agentproxy/status` で確認）。
   `raw.githubusercontent.com` だけは通るが、素性とライセンスの確認が要るものを
   規律を壊してまで入れる理由が無い。

**音は話が別。** 音は密度もパレットも持たないので外部素材と相性がいい。
ただし**この作品には既に11種の効果音が同梱されていて、5種しか鳴らしていない**。
買う前に配線する。

| 同梱されている音 | 使用状況 |
| --- | --- |
| `chest_open` `thunder` `ui_buy` `ui_confirm` `ui_denied` | 鳴っている |
| `damage` `enemy_death` `slash` `sword` `fire` `teleport` `ui_equip` | **一度も鳴っていない** |

---

## 一覧

凡例：**済**＝実装済み／**弱**＝あるが手当てが足りない／**無**＝何も起きない

### 朝（ホーム）

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 1 | 起動・店開き | 済 | — | 導入カメラ 2.6秒 ease-out |
| 2 | 店番を替える | 済 | 無 | 純益カウント＋差分フロート |
| 3 | 献立を入れ替える | 済 | 無 | 同上 |
| 4 | 扉の方針を変える | 済 | 無 | 同上 |
| 5 | 「集中する」を押す | 弱 | 無 | 画面フェードのみ。25分の始まりの儀式が無い |

### 潜航（25分）

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 6 | 敵の接近 | 済 | 無 | 奥から0.55秒 ease-out ＋土煙 |
| 7 | 命中 | 済 | 済 | `slash`/`sword` 交互。25分で約101回（間引き前241回） |
| 8 | 会心 | 済 | 済 | `sword` をピッチ1.16〜1.26で一段上に |
| 9 | 撃破 | 済 | 済 | `enemy_death`。雑魚/エリート/ボスで音量とピッチを変える。25分で89回（間引き前700回） |
| 10 | 被弾 | 済 | 済 | `damage` |
| 11 | 同期率レベルアップ | 済 | 済 | 新規生成 `sync_up`（上昇アルペジオ） |
| 12 | 共鳴の獲得（Lv3/6/9/12） | 済 | 済 | 新規生成 `resonance`（鐘＋シマー2.8秒）。前後を静かにして山を作る |
| 13 | 階層突破 | 済 | 済 | 新規生成 `floor_clear` |
| 14 | ボス出現 | 弱 | 無 | バナーはあるが接近演出と区別が薄い |
| 15 | 箱を拾う | 済 | 無 | アイコンが弧を描いて数字チップへ飛ぶ |
| 16 | 装備を発見（loot） | 弱 | 無 | 上部バナー＋ログ行＋仲間の一言。**文字だけで、物が出てこない**。等級も伝わらない |
| 17 | 記憶のかけら | 弱 | 無 | 同上。物語の断片なので、装備と同じ扱いでよいかは要検討 |
| 18 | 全滅→緊急再同期 | 弱 | 無 | ログのみ。25分が途切れない設計なので静かでよいが、何が起きたかは見せたい |
| 19 | スキル発動 | 弱 | 済 | `fx` 種別で `fire`/`thunder`/`teleport`/`heal`（新規）へ振り分け |

### 浮上・精算 ← **最大の穴**

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 20 | 三行精算の提示 | 済 | 無 | 売上を48pxでカウントアップ。タイポとパネルを他画面へ揃えた |
| 21 | **箱を開ける** | 済 | 無 | 等級ごとに 溜め→開封→着地（木0.26/鉄0.40/銀0.76/金1.22秒）。銀と金は暗転して舞台になり、金は光条14本＋48pxの名札。12個でも全部見え、全体7秒以内。タップで送れる。**音は未接続（コメントで位置を残してある）** |
| 22 | 入手した装備のレア度提示 | 済 | 無 | 等級で演出の格が変わるので引きの良し悪しが伝わる |
| 23 | デイリー達成・ご祝儀 | 弱 | 無 | ログ行のみ |

### 夜営業

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 24 | 開店 | 済 | 無 | |
| 25 | 客の入店・着席・配膳 | 済 | 無 | 6状態の連続動作＋皿の放物線とバウンド |
| 26 | タップ給仕・チップ | 済 | 済 | `ui_buy` が鳴る |
| 27 | 客が待ちきれず帰る | 済 | 無 | |
| 28 | 伝票が埋まる | 済 | 無 | 落ちてバウンドして入る |
| 29 | 締めの一幕 | 済 | 無 | 拭く→のれん→木札→暗転 |

### 経営

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 30 | 購入 | 済 | 済 | 所持金から数値が放物線で飛ぶ |
| 31 | 改装の解放 | 済 | 無 | ノードが爆ぜて前提から光の線が走る |
| 32 | 装備の着脱 | 済 | 済 | `ui_equip`。着ける/外す/嵌める/抜くでピッチを裏返す |
| 33 | 工房の合成 | 弱 | 無 | **★大成功★ があるのにログ文字だけ。** 引きの演出が無い |
| 34 | 分解・廃材化 | 弱 | 無 | |
| 35 | 親密度アップ・技の解放 | 弱 | 無 | |

### 横断

| # | 瞬間 | 視覚 | 音 | 備考 |
| --- | --- | --- | --- | --- |
| 36 | 画面遷移 | 済 | 無 | 0.32秒フェード＋要素のスタッガー |
| 37 | 会話 | 済 | 無 | |
| 38 | ストリーク更新（常連が増える） | 弱 | 無 | 数字は出るが祝われない |

---

## 優先順位

- [x] ~~#21 箱開封の作り直し~~ 完了
- [x] ~~音の配線~~ 完了（眠っていた7種＋新規4種）

残り：

1. **#21 の音。** 視覚は作り直したが音が繋がっていない。等級で音を変えると効く。
   コード中に `[SFX]` コメントで6箇所の位置を残してある。
2. **#16 装備発見／#17 記憶のかけら。** 上部バナーとログ行は出るが文字だけで、
   物が出てこない。潜航中に拾った実感が無い。
3. **#33 工房の大成功。** 確率で当たる要素に演出が無いのは損。
4. **#14 ボス出現／#18 全滅→緊急再同期。** 音は `SfxRouter` に2行足すだけで繋がる。
5. **#5 集中開始の儀式。** 25分の始まりに区切りを与える。
6. **音量・ミュート設定が存在しない。** ブラウザのタブミュートに頼っている。
   音を増やした以上、設定は要る。
7. **音量バランスは机上。** 頻度が高いほど絞る方針で決めた値で、実聴で詰めていない。
   調整点は `SfxRouter` の `vol` に集約してある。
