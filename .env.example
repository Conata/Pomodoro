# これをコピーして「.env」にし、自分のキーを入れる（.env は .gitignore 済み＝コミットされない）
#   cp .env.example .env  &&  エディタで PIXAI_API_KEY を記入
# tools/pixai_gen.py は環境変数 or この .env を自動で読む。
# 重要: キーをコミット/チャット貼り付けしない。露出したら pixai.art で必ず再発行。
PIXAI_API_KEY=
# 任意: 既定モデルIDを決めておくと --model 省略時に使われる
PIXAI_MODEL_ID=